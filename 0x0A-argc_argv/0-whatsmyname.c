#include <stdio.h>

/**
 * main - the master function
 * @argc: index of string argv
 * @argv: a string
 * Return: what is needed
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
