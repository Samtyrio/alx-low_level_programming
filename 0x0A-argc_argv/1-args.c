#include <stdio.h>

/**
 * main - the master function
 * @argc: the number man
 * @argv: the character man
 * Return: what is needed
 */

int main(int argc, char *argv[])
{
	(void) *argv;
	printf("%d\n", argc - 1);
	return (0);
}
